# OS 实验（lab0-lab2）

学号：2024302111357

本项目是在 RISC-V QEMU 环境下完成的操作系统实验，当前包含 lab0、lab1 和 lab2 的内容。

## 实验内容

- lab0：阅读 xv6 代码，整理 `echo hi` 的执行过程，完成控制流图、数据结构图和时钟中断流程图。
- lab1：完成内核启动、M 态到 S 态切换、UART 轮询输出和基础 `printf`，实现学号 Banner 输出。
- lab2：完成 trampoline 和 trap 处理、系统调用、UART 输入中断、输入缓冲、进程切换以及内嵌用户程序装载。

## 目录说明

```text
kernel/       内核启动、陷阱、系统调用、进程和串口代码
user/         Shell 及内嵌用户程序
tests/        lab2 测试程序
support/      串口测试脚本和测试说明
docs/lab0/    lab0 图纸和设计笔记
```

## 编译和运行

编译内核：

```bash
make clean
make
```

启动 QEMU：

```bash
make qemu
```

启动后进入 `sh>`，可运行：

```text
hi
spin
badecall
bufstorm
```

## 测试

检查 Banner 预期输出格式：

```bash
python3 check_expect.py 2024302111357
```

串口自动化测试：

```bash
python3 support/inject_uart.py --tree . --script support/storm.txt
```

lab2 的两个测试程序分别用于检查非法系统调用和输入缓冲区边界。

## 版本标签

```text
lab0-submit
lab1-start
lab1-submit
lab2-start
lab2-submit
```

实验归档包：

```text
提交-lab1-2024302111357.zip
提交-lab2-2024302111357.zip
```

当前 lab2 使用 `satp=0` 的直映方式，用户程序以平铺二进制形式嵌入内核镜像，暂未实现后续实验中的 SV39 页表和文件系统。
